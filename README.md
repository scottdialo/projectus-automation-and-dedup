# projectus-automation-and-dedup

This is an automation app that in return when work, should check for duplicates entry if none available then it create a new record in loxo db, same applies for companies, candidate/contact.

- This should also have webhook enable to listen and watch for all changes in loxo db realtime and triger our server if anything is dedected such as an employee add,update,delete a record so we can update our sqlite to maintain data integrity.
